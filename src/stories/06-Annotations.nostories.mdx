import { Meta } from '@storybook/addon-docs/blocks';
import { FunctionPlot, Render } from './FunctionPlot';
import functionPlot from '../';
window.functionPlot = functionPlot

<Meta title="Annotations" component={FunctionPlot} />

## Graph Annotations

Parallel lines to the y-axis or x-axis can be set in the `annotations` option:

- `x`: x coordinate of a line parallel to the y-axis
- `y`: y coordinate of a line parallel to the x-axis
- `text` (optional) text shown next to the parallel line

NOTE: either `x` or `y` need to be set on the object, setting both of them
will raise an exception

export const script = `
functionPlot({
  target: '#annotations',
  yAxis: {domain: [-1, 9]},
  data: [{
    fn: 'x^2'
  }],
  annotations: [{
    x: -1
  }, {
    x: 1,
    text: 'x = 1'
  }, {
    y: 2,
    text: 'y = 2'
  }]
})
`

<Render id="annotations" content={script}></Render>
